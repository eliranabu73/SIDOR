# --- Supabase Postgres ---
# Pooled (runtime, pgbouncer) — used by Prisma Client at request time
DATABASE_URL="postgresql://postgres.lpnqyzlfsosdxnykaaer:CHANGE_ME@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
# Direct (5432) — used by Prisma Migrate
DIRECT_URL="postgresql://postgres:CHANGE_ME@db.lpnqyzlfsosdxnykaaer.supabase.co:5432/postgres"
# Test schema in the same DB
TEST_DATABASE_URL="postgresql://postgres:CHANGE_ME@db.lpnqyzlfsosdxnykaaer.supabase.co:5432/postgres?schema=test"

# --- Supabase API ---
SUPABASE_URL="https://lpnqyzlfsosdxnykaaer.supabase.co"
SUPABASE_ANON_KEY="sb_publishable_n3OjpV8Bm8ydDO3HvqraWw_7bFJlCDR"

# --- Redis (locks, rules-snapshot cache, pubsub) ---
# Leave empty in dev to use the in-memory shim
REDIS_URL=""

# --- App ---
PORT="3000"
LOG_LEVEL="info"
NODE_ENV="development"

# --- Auth (placeholder — JWT/Supabase Auth integration later) ---
JWT_SECRET="change-me-in-prod"

# --- Platform admin ---
# Comma-separated list of platform admin emails (case-insensitive).
# These users get access to /admin — a cross-tenant dashboard for the SaaS
# owner that BYPASSES Row Level Security. Distinct from per-org OWNER role.
# Defaults to eliranabu320@gmail.com if unset.
PLATFORM_ADMIN_EMAILS="eliranabu320@gmail.com"
